models:
  Qwen/Qwen3.5-0.8B:
    provider_type: "vllm"
    api_base: "http://localhost:8001"
    max_generation_tokens: 4096 # make sure this is less than the model's max context length. Otherwise there will be no space left for the user prompt and the model will fail to generate a response.
    
  # gemini-1.5-pro:
  #   provider_type: "gemini"
  #   api_base: "https://generativelanguage.googleapis.com/v1beta"
  #   api_key_env_var: "GEMINI_API_KEY" # Look up the key from the OS environment to keep it secure
  #   max_generation_tokens: 8192