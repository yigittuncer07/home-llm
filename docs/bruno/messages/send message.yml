info:
  name: send message
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/chats/:chatId/messages"
  params:
    - name: chatId
      value: "21"
      type: path
  body:
    type: json
    data: |-
      {
        "model": "qwen",
        "prompt": "what is the meaning of life"
      }
  auth:
    type: bearer
    token: "{{bearer_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
