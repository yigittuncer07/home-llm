info:
  name: delete chat
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/admin/chats/:chatId"
  params:
    - name: chatId
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{bearer_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
