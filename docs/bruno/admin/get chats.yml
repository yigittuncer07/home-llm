info:
  name: get chats
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/admin/chats/:userId"
  params:
    - name: userId
      value: "24"
      type: path
  auth:
    type: bearer
    token: "{{bearer_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
