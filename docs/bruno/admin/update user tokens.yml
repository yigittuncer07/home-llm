info:
  name: update user tokens
  type: http
  seq: 7

http:
  method: PATCH
  url: "{{base_url}}/admin/users/:userId/tokens"
  params:
    - name: userId
      value: "25"
      type: path
  body:
    type: json
    data: |-
      {
        "model_name": "Qwen/Qwen3.5-0.8B",
        "balance": "5000"
      }
  auth:
    type: bearer
    token: "{{bearer_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
