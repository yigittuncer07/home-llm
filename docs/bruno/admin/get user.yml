info:
  name: get user
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/admin/users/:userId"
  params:
    - name: userId
      value: "25"
      type: path
  auth:
    type: bearer
    token: "{{bearer_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
