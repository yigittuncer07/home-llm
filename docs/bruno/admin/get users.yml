info:
  name: get users
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/admin/users"
  auth:
    type: bearer
    token: "{{bearer_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
