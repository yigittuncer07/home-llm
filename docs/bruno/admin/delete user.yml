info:
  name: delete user
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{base_url}}/admin/users/:userId"
  params:
    - name: userId
      value: "27"
      type: path
  auth:
    type: bearer
    token: "{{bearer_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
