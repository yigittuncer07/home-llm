info:
  name: register
  type: http
  seq: 6

http:
  method: POST
  url: "{{base_address}}/admin/users"
  body:
    type: json
    data: |-
      {
        "email": "t3@t.com",
        "password": "pass"
      }
  auth:
    type: bearer
    token: "{{bearer_token}}"

runtime:
  variables:
    - name: base_address
      value: http://0.0.0.0:8007

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
