info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_address}}/auth/login"
  auth: inherit

runtime:
  variables:
    - name: base_address
      value: http://0.0.0.0:8007

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
