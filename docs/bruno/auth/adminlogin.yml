info:
  name: adminlogin
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "admin@admin.com",
        "password": "admin"
      }
  auth: inherit

runtime:
  variables:
    - name: base_address
      value: http://0.0.0.0:8007

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
