info:
  name: logout
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_address}}/auth/logout"
  auth: inherit

runtime:
  variables:
    - name: base_address
      value: http://0.0.0.0:8007

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
