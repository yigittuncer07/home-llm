info:
  name: create chat
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/chats"
  auth:
    type: bearer
    token: "{{bearer_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
