info:
  name: delete chat
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{base_url}}/chat/:chatId"
  params:
    - name: chatId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
