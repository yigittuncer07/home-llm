info:
  name: patch chat
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{base_url}}/chat/:chatId"
  params:
    - name: chatId
      value: "24"
      type: path
  body:
    type: json
    data: |-
      {
        "title": "updated title 1"
      }
  auth:
    type: bearer
    token: "{{bearer_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
